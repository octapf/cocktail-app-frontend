import Container from '../components/Container/Container'

const Favoritespage = () => {
	return (
		<Container typeContainer={'container-favorites'}>Favoritespage</Container>
	)
}

export default Favoritespage
